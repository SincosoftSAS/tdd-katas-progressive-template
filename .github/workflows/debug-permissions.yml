name: 🔍 Debug Permissions

on:
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: write
  actions: write

jobs:
  debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 🔧 Setup GitHub CLI
        run: |
          gh auth login --with-token <<< "${{ secrets.GITHUB_TOKEN }}"

      - name: 🔍 Check permissions
        run: |
          echo "🔍 Verificando permisos..."
          
          # Buscar específicamente repos katas-tdd
          echo "📋 Repos katas-tdd encontrados:"
          gh repo list ${{ github.repository_owner }} \
            --json name \
            --jq '.[] | select(.name | contains("katas-tdd")) | select(.name != "tdd-katas-progressive-template") | select(.name | endswith("-template") | not) | .name' \
            > student_repos.txt
          
          TOTAL=$(cat student_repos.txt | wc -l)
          echo "✅ Total encontrados: $TOTAL repos"
          
          cat student_repos.txt | head -5 | while read repo; do
            [ -n "$repo" ] && echo "  📁 ${{ github.repository_owner }}/$repo"
          done
          
          echo ""
          echo "🔑 Token info:"
          gh auth status
          
          echo ""
          echo "👤 Usuario actual:"
          gh api user --jq '.login'
          
          echo ""
          echo "🏢 Permisos en organización:"
          gh api "orgs/${{ github.repository_owner }}/members" --jq '.[].login' | head -3 || echo "No acceso a lista de miembros"

      - name: 🎯 Test specific student repos
        run: |
          echo "🎯 Probando acceso a repos estudiantes específicos..."
          
          # Probar con los primeros 3 repos encontrados
          cat student_repos.txt | head -3 | while read repo; do
            [ -z "$repo" ] && continue
            
            echo ""
            echo "� Probando: $repo"
            echo "================================"
            
            # Verificar acceso básico al repo
            if gh repo view "${{ github.repository_owner }}/$repo" --json name,owner,visibility,permissions >/dev/null 2>&1; then
              echo "  ✅ Acceso de lectura: OK"
              
              # Verificar permisos específicos
              PERMS=$(gh repo view "${{ github.repository_owner }}/$repo" --json permissions --jq '.permissions')
              echo "  � Permisos: $PERMS"
              
              # Intentar verificar si puede crear PRs
              if echo "$PERMS" | grep -q "\"push\":true"; then
                echo "  ✅ Permisos de push: OK"
              else
                echo "  ❌ Sin permisos de push"
              fi
              
              if echo "$PERMS" | grep -q "\"pull\":true"; then
                echo "  ✅ Permisos de pull: OK"
              else
                echo "  ❌ Sin permisos de pull"
              fi
              
            else
              echo "  ❌ Sin acceso al repo"
            fi
            
            sleep 1
          done
          
          echo ""
          echo "📊 Resumen del diagnóstico completado"
          
          echo ""
          echo "🔍 Análisis de token GitHub:"
          echo "Token scope: ${{ github.token }}" | cut -c1-20
          echo "Repo actual: ${{ github.repository }}"
          echo "Actor: ${{ github.actor }}"
          echo "Event: ${{ github.event_name }}"